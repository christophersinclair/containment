[response]
threshold = 3            # Number of events before auto-mitigating
kill_on_escape = true    # Automatically kill a container if escape detected
quarantine = true        # Isolate the container using cgroups
notify_only = false      # If true, only log and alert instead of taking action
